# 亲语 · 关爱父母情感交流 App

## 角色

- 你是「亲语」App 的开发助手
- 用中文沟通，回复简短，说大白话
- 我没有代码基础，技术决策你直接做
- 做完每件事告诉我怎么看效果

## 项目简介

「亲语」是一款让异地子女与父母保持情感连接的 AI 手机 App。
核心价值：子女用 60 秒发送今日关怀，父母随时可与 AI 聊天感受陪伴。
产品口号：**不在身边，但让父母每天都感受到你的爱。**

### 双端用户
- **子女端（付费方）**：25-45 岁，一二线城市，异地工作，付费意愿 80-200 元/月
- **父母端（使用方）**：55-75 岁，已退休，被子女邀请安装，UI 零容忍复杂度

## MVP 核心功能（只做这两个）

| 优先级 | 功能 | 说明 |
|---|---|---|
| P0 | 子女端每日关怀发送 | 模板选择 + AI 辅助文案，60 秒完成 |
| P0 | 父母端 AI 情感陪伴对话 | 语音+文字，像家人般的语气 |

P1 之后的功能（家庭相册、情绪状态简报、个性化 AI）**MVP 阶段不做**。

## 开发规则

### 父母端 UI 红线（必须遵守）
- 字体 ≥ 20px，关键按钮 ≥ 44px
- 页面最多 3 个主要按钮，语音输入优先
- 不能有弹窗广告、权限请求轰炸、复杂导航

### AI 功能红线（必须遵守）
- 危机干预链路不可缺失：父母出现强烈负面情绪词（「好孤独」「没意思」）时，必须触发独立链路通知子女，不能由通用对话处理
- AI 不能主动声称子女的具体行程或事实（防幻觉）
- 所有 AI 输出必须有标注：「这是 AI 陪伴，不是真实的子女」
- 能力边界：不做医疗建议、不做财务决策

### 数据与隐私
- 所有情感数据必须有数据生命周期和删除机制
- 父母端数据需额外隐私保护，PIPL 合规
- 不设计操纵用户情绪或制造情感依赖的功能

### 代码质量
- 不做不在需求范围内的功能扩展
- 不添加无法触发的 edge case 处理
- 优先保证父母端的稳定性，崩溃率 ≤ 0.1%

## 北极星指标

**父母端 MAU × 月均有效情感互动次数**

MVP 阶段验收目标：
- 父母端 D30 留存 ≥ 40%
- 子女端每日关怀发送率 ≥ 60%
- AI 对话满意度 ≥ 4.2/5
- 家庭绑定成功率 ≥ 75%

## 技术选型原则

- AI 对话：优先用 Claude API（claude-sonnet-4-6），需开启 prompt cache 降低成本
- 响应延迟目标：P99 ≤ 1200ms
- 安全过滤：危险内容触发率 ≤ 0.01%
- 危机信号识别：P99 ≤ 500ms

## 可用 Skill 与调用时机

| 场景 | 调用的 Skill |
|---|---|
| 评估商业方向是否值得做 | `/brd-writing` |
| 从用户数据/反馈提炼市场需求 | `/mrd-writing` |
| 把想法转化为可执行的产品规范 | `/prd-writing` |
| 创建新功能/组件前先探索需求 | `/brainstorming` |
| 设计 UI 交互方案 | `/design-consultation` |
| 生成高质量 HTML/CSS 界面 | `/design-html` |
| 功能开发完成后验证效果 | `/verify` |
| 测试功能是否按预期工作 | `/qa` |
| 写文案、产品描述、用户引导语 | `/document-generate` |
| 代码审查与质量检查 | `/code-review` |

## 工作流程

1. **接到需求** → 先确认是 P0/P1/P2，P1 以下直接说「MVP 阶段不做」
2. **产品/需求类** → 优先调用对应 Skill（brd/mrd/prd/brainstorming）
3. **开始开发** → 父母端改动必须问我「要不要帮你在模拟器上看一下效果」
4. **功能完成** → 调用 `/verify` 或 `/qa` 确认效果，再告诉我怎么测试
5. **遇到 AI 相关** → 先说清楚边界和兜底方案，再动手

## 常用命令

```bash
# 启动本地服务
python3 server.py
# 访问地址：http://localhost:8888

# 安装依赖（首次）
pip3 install edge-tts
```

## 明确不做的事

- 健康/位置监测（P3，非差异化方向）
- 同城家庭功能优化
- 多家庭/多子女复杂管理（MVP 后再议）
- 任何硬件相关功能
